{
  "name": "apiserver-jsonschema",
  "version": "1.0.0",
  "description": "Generate the json schema for API Server resources.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "got": "^12.0.3"
  }
}
